<UserControl x:Class="AdmissionSystem.Views.RankingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:AdmissionSystem.Converters">

    <UserControl.Resources>
        <conv:RecommendationToColorConverter x:Key="RecommendationToColorConverter"/>

        <Style x:Key="CenterAlignStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnPrimary" BasedOn="{StaticResource ActionButton}" TargetType="Button">
            <Setter Property="Background" Value="#007AFF"/>
        </Style>
        <Style x:Key="BtnSecondary" BasedOn="{StaticResource ActionButton}" TargetType="Button">
            <Setter Property="Background" Value="#F2F2F7"/>
            <Setter Property="Foreground" Value="#1C1C1E"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowHeight" Value="52"/>
            <Setter Property="ColumnHeaderHeight" Value="48"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E5E5EA"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#8E8E93"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="16,0,0,0"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E5E5EA"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="16,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="Transparent" Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FFFFFF" CornerRadius="24" Padding="24" Margin="0,0,0,24">
            <Border.Effect>
                <DropShadowEffect BlurRadius="40" ShadowDepth="10" Opacity="0.04" Color="#001530"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Спеціальність:" VerticalAlignment="Center" Foreground="#8E8E93" FontSize="14" Margin="0,0,12,0"/>
                <ComboBox Grid.Column="1" ItemsSource="{Binding Specialties}" SelectedItem="{Binding SelectedSpecialty}"
                          DisplayMemberPath="Name" Height="40" FontSize="14" VerticalContentAlignment="Center"/>

                <TextBlock Grid.Column="2" Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="16,0,0,0"
                           Foreground="#8E8E93" FontSize="13"/>

                <Button Grid.Column="3" Style="{StaticResource BtnSecondary}" Command="{Binding RefreshRankingCommand}" Margin="0,0,12,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Оновити" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Grid.Column="4" Style="{StaticResource BtnPrimary}" Command="{Binding RecalculateCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE945;" FontFamily="Segoe Fluent Icons" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Перерахувати рейтинг" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="#FFFFFF" CornerRadius="16" Padding="16,12" Margin="0,0,0,16" BorderBrush="#E5E5EA" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="&#xE946;" FontFamily="Segoe Fluent Icons" Foreground="#8E8E93" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="Легенда:" Foreground="#8E8E93" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,16,0"/>

                <Border Background="#34C759" CornerRadius="8" Padding="12,4" Margin="0,0,12,0">
                    <TextBlock Text="Бюджет" Foreground="White" FontSize="12" FontWeight="Bold"/>
                </Border>
                <Border Background="#007AFF" CornerRadius="8" Padding="12,4" Margin="0,0,12,0">
                    <TextBlock Text="Контракт" Foreground="White" FontSize="12" FontWeight="Bold"/>
                </Border>
                <Border Background="#FF9500" CornerRadius="8" Padding="12,4">
                    <TextBlock Text="Резерв" Foreground="White" FontSize="12" FontWeight="Bold"/>
                </Border>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="#FFFFFF" CornerRadius="16" BorderBrush="#E5E5EA" BorderThickness="1" ClipToBounds="True">
            <DataGrid ItemsSource="{Binding Ranking}">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Recommendation}" Value="Бюджет">
                                <Setter Property="Background" Value="#E5F9EA"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Recommendation}" Value="Контракт">
                                <Setter Property="Background" Value="#E5F0FF"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Recommendation}" Value="Резерв">
                                <Setter Property="Background" Value="#FFF5E5"/>
                            </DataTrigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding Rank}" Width="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="ExtraBold"/>
                                <Setter Property="FontSize" Value="15"/>
                                <Setter Property="Foreground" Value="#1C1C1E"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="АБІТУРІЄНТ" Binding="{Binding FullName}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Medium"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="БАЛ" Binding="{Binding CompetitiveScore, StringFormat=F2}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="ПРІОРИТЕТ" Binding="{Binding Priority}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="#8E8E93"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="РЕКОМЕНДАЦІЯ" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding Recommendation, Converter={StaticResource RecommendationToColorConverter}}"
                                        CornerRadius="8" Padding="12,4" Margin="0,0,16,0" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Recommendation}" Foreground="White" FontWeight="Bold" FontSize="12"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="СТАТУС ЗАЯВИ" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding Application.CurrentStatus, Converter={StaticResource StatusToColorConverter}}"
                                        CornerRadius="8" Padding="12,4" Margin="0,0,16,0" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Application.CurrentStatus, Converter={StaticResource StatusToStringConverter}}"
                                               Foreground="{Binding Application.CurrentStatus, Converter={StaticResource StatusToForegroundConverter}}"
                                               FontSize="12" FontWeight="Bold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>