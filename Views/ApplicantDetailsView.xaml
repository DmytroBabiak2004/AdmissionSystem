<UserControl x:Class="AdmissionSystem.Views.ApplicantDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="Segoe UI"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PageBackgroundBrush" Color="#F6F8FC"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SurfaceSoftBrush" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="SurfaceMutedBrush" Color="#F1F5F9"/>
        <SolidColorBrush x:Key="BorderSoftBrush" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="BorderStrongBrush" Color="#CBD5E1"/>

        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#0F172A"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#64748B"/>
        <SolidColorBrush x:Key="TextMutedBrush" Color="#94A3B8"/>

        <SolidColorBrush x:Key="PrimaryBrush" Color="#2563EB"/>
        <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#1D4ED8"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#16A34A"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#D97706"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#DC2626"/>
        <SolidColorBrush x:Key="DangerSoftBrush" Color="#FEF2F2"/>
        <SolidColorBrush x:Key="SuccessSoftBrush" Color="#ECFDF3"/>
        <SolidColorBrush x:Key="WarningSoftBrush" Color="#FFF7ED"/>
        <SolidColorBrush x:Key="PrimarySoftBrush" Color="#EFF6FF"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        </Style>

        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="2,0,0,8"/>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="SurfaceCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoftBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="20"/>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="44"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.96"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.88"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnPrimary" BasedOn="{StaticResource ActionButton}" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        </Style>

        <Style x:Key="BtnSecondary" BasedOn="{StaticResource ActionButton}" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoftBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="IconBtnAction" TargetType="Button">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="{StaticResource SurfaceMutedBrush}"/>
                                <Setter TargetName="bg" Property="BorderBrush" Value="{StaticResource BorderSoftBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="{StaticResource BorderSoftBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowHeight" Value="60"/>
            <Setter Property="ColumnHeaderHeight" Value="48"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderSoftBrush}"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource SurfaceSoftBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoftBrush}"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SurfaceSoftBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimarySoftBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,0,8,12"/>
            <Setter Property="Margin" Value="0,0,24,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid>
                            <ContentPresenter ContentSource="Header" Margin="{TemplateBinding Padding}" HorizontalAlignment="Center"/>
                            <Border x:Name="Indicator" Height="3" Background="Transparent" CornerRadius="2" VerticalAlignment="Bottom"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="Indicator" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <Grid Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource SurfaceCard}" Padding="32" Margin="0,0,0,28">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="4" Opacity="0.04" Color="#0F172A"/>
                </Border.Effect>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Width="88" Height="88" CornerRadius="44" Background="{StaticResource SurfaceMutedBrush}" BorderBrush="{StaticResource BorderSoftBrush}" BorderThickness="1" Margin="0,0,24,0">
                        <TextBlock Text="&#xE77B;" FontFamily="Segoe Fluent Icons" FontSize="38" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Applicant.FullName}" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                        <WrapPanel Orientation="Horizontal">
                            <Border Background="{StaticResource SurfaceSoftBrush}" CornerRadius="10" Padding="12,8" Margin="0,0,12,12">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE787;" FontFamily="Segoe Fluent Icons" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Applicant.DateOfBirth, StringFormat=dd.MM.yyyy}" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <Border Background="{StaticResource SurfaceSoftBrush}" CornerRadius="10" Padding="12,8" Margin="0,0,12,12">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE715;" FontFamily="Segoe Fluent Icons" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Applicant.Email}" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <Border Background="{StaticResource SurfaceSoftBrush}" CornerRadius="10" Padding="12,8" Margin="0,0,12,12">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE717;" FontFamily="Segoe Fluent Icons" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Applicant.Phone}" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <Border Background="{StaticResource WarningSoftBrush}" CornerRadius="10" Padding="12,8" Margin="0,0,0,12">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE73D;" FontFamily="Segoe Fluent Icons" Foreground="{StaticResource WarningBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Applicant.AverageGrade, StringFormat='Середній бал: {0:F2}'}" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource WarningBrush}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </WrapPanel>
                    </StackPanel>

                    <Button Grid.Column="2" Style="{StaticResource BtnSecondary}" Click="BackButton_Click" VerticalAlignment="Top">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE72B;" FontFamily="Segoe Fluent Icons" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="14"/>
                            <TextBlock Text="До списку" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <TabControl Grid.Row="1">
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE8F1;" FontFamily="Segoe Fluent Icons" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Заяви" VerticalAlignment="Center"/>
                        </StackPanel>
                    </TabItem.Header>

                    <Grid Margin="0,24,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Button Grid.Row="0" Style="{StaticResource BtnPrimary}" Command="{Binding AddApplicationCommand}" HorizontalAlignment="Left" Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE710;" FontFamily="Segoe Fluent Icons" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Нова заява" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Border Grid.Row="1" Style="{StaticResource SurfaceCard}" CornerRadius="16" ClipToBounds="True">
                            <DataGrid ItemsSource="{Binding Applications}" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="#" Binding="{Binding Priority}" Width="56"/>
                                    <DataGridTextColumn Header="СПЕЦІАЛЬНІСТЬ" Binding="{Binding Specialty.Name}" Width="2.5*"/>
                                    <DataGridTextColumn Header="КОД" Binding="{Binding Specialty.Code}" Width="90"/>
                                    <DataGridTextColumn Header="ФОРМА" Binding="{Binding FormOfEducation, Converter={StaticResource FormToStringConverter}}" Width="120"/>
                                    <DataGridTextColumn Header="ОСНОВА" Binding="{Binding EducationBasis, Converter={StaticResource BasisToStringConverter}}" Width="120"/>
                                    <DataGridTextColumn Header="БАЛ" Binding="{Binding CompetitiveScore, StringFormat=F2}" Width="90"/>
                                    <DataGridTemplateColumn Header="СТАТУС" Width="180">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Background="{Binding CurrentStatus, Converter={StaticResource StatusToColorConverter}}" CornerRadius="6" Padding="12,6" HorizontalAlignment="Left">
                                                    <TextBlock Text="{Binding CurrentStatus, Converter={StaticResource StatusToStringConverter}}" Foreground="{Binding CurrentStatus, Converter={StaticResource StatusToForegroundConverter}}" FontSize="12" FontWeight="SemiBold"/>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="ДАТА" Binding="{Binding SubmissionDate, StringFormat=dd.MM.yyyy}" Width="110"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                    </Grid>
                </TabItem>

                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE8A5;" FontFamily="Segoe Fluent Icons" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Документи" VerticalAlignment="Center"/>
                        </StackPanel>
                    </TabItem.Header>

                    <Grid Margin="0,24,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Button Grid.Row="0" Style="{StaticResource BtnPrimary}" Command="{Binding AddDocumentCommand}" HorizontalAlignment="Left" Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE710;" FontFamily="Segoe Fluent Icons" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Додати документ" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Border Grid.Row="1" Style="{StaticResource SurfaceCard}" CornerRadius="16" ClipToBounds="True">
                            <DataGrid ItemsSource="{Binding Documents}" SelectedItem="{Binding SelectedDocument}" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="ТИП ДОКУМЕНТА" Binding="{Binding DocumentType, Converter={StaticResource DocTypeToStringConverter}}" Width="250"/>
                                    <DataGridTextColumn Header="НАЗВА ФАЙЛУ" Binding="{Binding FileName}" Width="*"/>
                                    <DataGridTemplateColumn Header="СТАТУС" Width="150">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Background="{StaticResource SurfaceSoftBrush}" CornerRadius="6" Padding="12,6" HorizontalAlignment="Left">
                                                    <TextBlock Text="{Binding Converter={StaticResource DocStatusConverter}}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="ПОДАНО" Width="100">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Text" Value="Ні"/>
                                                            <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsProvided}" Value="True">
                                                                    <Setter Property="Text" Value="Так"/>
                                                                    <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="ПЕРЕВІРЕНО" Width="140">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding IsVerified, Converter={StaticResource BoolToVerifiedTextConverter}}"
                                                           Foreground="{Binding IsVerified, Converter={StaticResource BoolToVerifiedColorConverter}}"
                                                           FontWeight="SemiBold"
                                                           VerticalAlignment="Center"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="ДІЇ" Width="120">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                    <Button Style="{StaticResource IconBtnAction}" 
                                                            Command="{Binding DataContext.EditDocumentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                            CommandParameter="{Binding}"
                                                            ToolTip="Редагувати документ">
                                                        <TextBlock Text="&#xE70F;" FontFamily="Segoe Fluent Icons" FontSize="15" Foreground="{StaticResource TextSecondaryBrush}"/>
                                                    </Button>

                                                    <Button Style="{StaticResource IconBtnAction}" 
                                                            Command="{Binding DataContext.DeleteDocumentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                            CommandParameter="{Binding}"
                                                            ToolTip="Видалити документ"
                                                            Margin="0">
                                                        <TextBlock Text="&#xE74D;" FontFamily="Segoe Fluent Icons" FontSize="15" Foreground="{StaticResource DangerBrush}"/>
                                                    </Button>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>

    <Border Grid.RowSpan="2" Margin="-32" Background="#99000000" Panel.ZIndex="20"
                Visibility="{Binding IsAddDocumentVisible, Converter={StaticResource BoolToVisibilityConverter}}">
        <Border Width="500" Background="#FFFFFF" CornerRadius="24" Padding="32" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="60" ShadowDepth="20" Opacity="0.15" Color="Black"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock FontSize="24" FontWeight="Bold" Foreground="#1C1C1E" Margin="0,0,0,24">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="Новий документ"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEditingExistingDocument}" Value="True">
                                    <Setter Property="Text" Value="Редагування документа"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <TextBlock Style="{StaticResource FormLabel}" Text="Тип документа *"/>
                <ComboBox ItemsSource="{Binding DocumentTypes}" SelectedItem="{Binding EditingDocument.DocumentType}" Height="40" FontSize="14" Margin="0,0,0,12">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource DocTypeToStringConverter}}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Style="{StaticResource FormLabel}" Text="Назва документа / Файл *"/>
                <TextBox Text="{Binding EditingDocument.FileName, UpdateSourceTrigger=PropertyChanged}" Height="40" Margin="0,0,0,16"/>

                <StackPanel Orientation="Horizontal" Margin="4,0,0,32">
                    <CheckBox Content="Оригінал подано" IsChecked="{Binding EditingDocument.IsProvided}" 
                                  Foreground="#1C1C1E" FontSize="14" FontWeight="Medium" Margin="0,0,24,0"/>
                    <CheckBox Content="Перевірено" IsChecked="{Binding EditingDocument.IsVerified}" 
                                  Foreground="#1C1C1E" FontSize="14" FontWeight="Medium"/>
                </StackPanel>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Скасувати" Style="{StaticResource BtnSecondary}" Command="{Binding CancelDocumentCommand}" Margin="0,0,8,0"/>
                    <Button Grid.Column="1" Content="Зберегти" Style="{StaticResource BtnPrimary}" Command="{Binding SaveDocumentCommand}" Margin="8,0,0,0"/>
                </Grid>
            </StackPanel>
        </Border>
    </Border>

        <Border Grid.RowSpan="2" Margin="-32" Background="#99000000" Panel.ZIndex="10"
        Visibility="{Binding IsAddApplicationVisible, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border Width="500" Background="#FFFFFF" CornerRadius="24" Padding="32" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="60" ShadowDepth="20" Opacity="0.15" Color="Black"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Нова заява" FontSize="24" FontWeight="Bold" Foreground="#1C1C1E" Margin="0,0,0,24"/>

                    <TextBlock Style="{StaticResource FormLabel}" Text="Спеціальність *"/>
                    <ComboBox ItemsSource="{Binding Specialties}" SelectedValue="{Binding NewApplication.SpecialtyId}"
                      SelectedValuePath="Id" DisplayMemberPath="Name" Height="40" FontSize="14" Margin="0,0,0,12"/>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Style="{StaticResource FormLabel}" Text="Форма навчання"/>
                            <ComboBox SelectedIndex="{Binding NewApplication.FormOfEducation, Converter={StaticResource EnumToIntConverter}}" Height="40" FontSize="14">
                                <ComboBoxItem Content="Денна"/>
                                <ComboBoxItem Content="Заочна"/>
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Style="{StaticResource FormLabel}" Text="Основа навчання"/>
                            <ComboBox SelectedIndex="{Binding NewApplication.EducationBasis, Converter={StaticResource EnumToIntConverter}}" Height="40" FontSize="14">
                                <ComboBoxItem Content="Бюджет"/>
                                <ComboBoxItem Content="Контракт"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Style="{StaticResource FormLabel}" Text="Пріоритет"/>
                            <TextBox Text="{Binding NewApplication.Priority, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Style="{StaticResource FormLabel}" Text="Конкурсний бал"/>
                            <TextBox Text="{Binding NewApplication.CompetitiveScore, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Style="{StaticResource FormLabel}" Text="Примітки"/>
                    <TextBox Text="{Binding NewApplication.Notes, UpdateSourceTrigger=PropertyChanged}"
                     Height="80" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,32"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="Скасувати" Style="{StaticResource BtnSecondary}" 
                        Command="{Binding CancelApplicationCommand}" Margin="0,0,8,0"/>
                        <Button Grid.Column="1" Content="Зберегти" Style="{StaticResource BtnPrimary}" 
                        Command="{Binding SaveApplicationCommand}" Margin="8,0,0,0"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</UserControl>